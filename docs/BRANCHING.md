# Branching

- `main`: stable / release-ready code.
- `develop`: active integration branch.
- `feature/*`: isolated feature work branched from `develop`.

Merge tested features into `develop`, then promote a tested release candidate to `main`.
