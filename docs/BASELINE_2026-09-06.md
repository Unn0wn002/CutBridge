# CutBridge Session 1 baseline — 2026-09-06

## Scope and source of truth

Repository: private `Unn0wn002/CutBridge`. Product scope remains the Blender → After Effects animation-cut production handoff, aimed primarily at Japanese artists/studios with English usability retained. Session 1 changes only release tooling, repository hygiene, tests and documentation. It does not implement render mapping, AE contract changes, revisions, QC features or a release.

The live `main` and `develop` tips were identical at `e282ef99b3fa5772b3d6d1dbbcbfa4957816b78c`, with tree `aa3bf5407b3d947f35d29d465dcebc017bbe697b`. GitHub compare reported 0 ahead / 0 behind. All 34 tracked file blobs, their modes, the tree and the commit were verified when materializing the local checkout through the authenticated GitHub connector. Direct CLI cloning lacked credentials; no source was made public.

Session branch: `fix/session-1-baseline-reconciliation`, created at that exact develop tip. After its PR is merged, develop intentionally contains Session 1 changes ahead of stable main. A separate release promotion must reconcile that difference; do not reset main or develop.

## Live repository inventory

- Default branch: `main`; repository visibility: private.
- Seven pre-session branches; no open PRs. PRs #3–#8 are merged, including sync PR #6, closed on 2026-09-06.
- Open issue #1: v0.2 extension/update foundation. Keep open: its acceptance criteria include a real tagged release, which has not occurred.
- No tag refs in the complete Git refs collection and no GitHub releases.
- Latest develop CI: [run 34024512593](https://github.com/Unn0wn002/CutBridge/actions/runs/34024512593), success at the baseline SHA. Latest main CI: [run 33652310116](https://github.com/Unn0wn002/CutBridge/actions/runs/33652310116), success at the same SHA.
- Earlier failures at `ff754c4` were followed by successful corrected runtime runs at `02b9871`; they are historical, not current failures.
- About description and homepage: null; topics: empty. License detection: null before root LICENSE was added. There is no repository-metadata write capability in the connected GitHub toolset. Suggested description: “Blender → After Effects animation-cut pipeline: metadata, pass packaging, versioning, JSON handoff and compositing setup. Built for Japanese creators with English usability.” Suggested topics: `blender`, `after-effects`, `animation`, `pipeline`, `japanese`.
- Main/develop branch responses report `protected: false`. Ruleset inspection returned GitHub's explicit 403 plan restriction. Do not make the private repository public to enable this feature; PR/green-CI discipline remains required.

## Old branch reconciliation

GitHub three-dot comparisons show divergent history because earlier PRs were squashed. Direct recursive file-tree comparisons provide the integration evidence below. Every old ref was retained; none was reset, deleted, or merged again.

| Retained branch | Exact tip | Integration comparison | Result |
|---|---|---|---|
| `archive/develop-pre-sync-20260906` | `0adee3cebb09808122a0e37164e7204830e3a610` | `93455d75799ca9c6f4c57ccab3c004668ff5dea2` (PR #4) | Identical files |
| `feature/v0.2-update-foundation` | `8291c6b455edd250d4882cfe6dd9c6faa51a3da7` | `93455d75799ca9c6f4c57ccab3c004668ff5dea2` (PRs #3/#4) | Only `docs/BLENDER_5_2_FIX_NOTE.md`, containing `temporary`, differs; preserve on old branch |
| `fix/blender-5.2-registration` | `00ad22fbeeff4a7861b8c9adbc5e92ce5a27ebb6` | `706250e2518f8a196f20013b39366f8e663080c2` (PR #5) | Identical files |
| `fix/blender-5.2-rna-runtime` | `02b9871b648f31dd51711c1521935c12b4087f50` | `fba068f9ed4b41e5ef697905a1476b2a991fad11` (PR #7) | Identical files |
| `fix/v0.2.3-manifest-and-testing` | `7e96082b82aff5028711c0f2b35aa490e3d298d8` | `12d0ad21966a83d7f4856625e66e75e0273cefab` (PR #8) | Identical files |

No production fix is waiting on an old branch. The remaining unique temporary note is explicitly accounted for and preserved.

## Version and schema audit

- Product version is `0.2.3`: `version.py` string/tuple and Blender manifest agree; `bl_info` imports the tuple; `core.build_manifest` imports the string for generated `cutbridge_version`.
- Release builder derives filenames, checksums and metadata from the matching tag/manifest and now rejects canonical string/tuple mismatch before output writes.
- README and latest changelog entry match 0.2.3. The missing 0.2.2 history was restored from live source/PR evidence; no historic release date was invented.
- The release-index example now uses 0.2.3 consistently but keeps invalid placeholder URLs and zero checksum. Historical update-selection test versions remain deliberate fixtures.
- Blender extension schema is `1.0.0`; handoff schema/current emitted version is integer 1; update/release metadata schema is integer 1. These are separate contracts.
- Both JSON schemas receive Draft 2020-12 meta-schema checks; the release-index example is validated. Blender integration validates actual generated handoff JSON.
- AE's header/footer still contains MVP v0.1 labels. This is a confirmed source-label discrepancy deliberately reserved for the AE session; the historical v0.1 guide is now marked historical. No AE source bytes changed in Session 1.

## Confirmed defects repaired

1. Missing root LICENSE despite the established `SPDX:GPL-3.0-or-later` Blender declaration. Added the full GPL v3 text and inclusion in both release ZIPs.
2. Claimed deterministic ZIPs used source timestamps/permissions. Reproduced unequal ZIP bytes after changing only timestamps; fixed archive metadata and added a repeat-build regression.
3. Release builder recursively deleted everything in its output directory. Reproduced deletion of an unrelated marker in a temporary fixture. It now rejects unrelated entries, artifact symlinks and source overlap before writing.
4. Builder accepted a divergent `VERSION` tuple when the tag and manifest matched. Added AST-based constant verification and a rejection regression.
5. Missing 0.2.2 changelog and stale v0.2.1 render-mapping roadmap. Reconciled history and future milestones without claiming feature completion.
6. `dist-ci/` was not ignored. Added build-output hygiene and explicit checksum verification to CI/release workflow.

## Validation evidence and boundaries

| Check | Evidence/status |
|---|---|
| Existing baseline static suite | PASS: 8 tests in baseline develop CI job 101462982550 |
| Existing Blender integration suite | PASS: 12 tests in baseline develop CI job 101462982560 |
| Official bpy lifecycle | PASS in baseline CI logs: `bpy version: 5.2.1 LTS`, two cycles, explicit lifecycle PASS |
| Python compilation | PASS locally with Python 3.12.13 |
| JSX syntax | PASS locally using Node parser; no AE runtime claim |
| New release safety regressions | Reproduced failures before repair; PASS after repair, 7 unittest cases |
| Fresh complete pytest/bpy execution | Required on the session PR. The updated CI runs the whole suite plus the standalone lifecycle script; use that PR's exact revision logs as final evidence |
| Local pytest/bpy execution | BLOCKED: packages are absent and dependency installation was blocked in this environment; no tests are skipped or substituted in CI |
| Release publication | MANUAL / later release session: no tag or GitHub release has exercised publishing |
| Blender GUI | User reports prior successful 5.2.1 install, N-panel, Validate Cut and Build Package. Not independently rerun in Session 1 |
| AE GUI and full handoff | MANUAL TEST REQUIRED; no real-app run in Session 1 |

The existing static and Blender tests are preserved and strengthened. AE archive membership now explicitly requires the additional LICENSE and still verifies exact script bytes. Node parsing and ZIP simulation cannot certify AE behavior.

## Deferred work and exact next session

**Session 2 — Blender Render-Pass / Output Mapping.** Start by inspecting current develop and branching from its live tip. Reproduce the absence of Blender output mapping; implement mapping of selected package passes to explicit render outputs/View Layers, deterministic paths and validation. Preserve the current JSON handoff contract unless an intentional compatible change is necessary. Add meaningful bpy integration coverage, run the complete suite/lifecycle/JSX/release gates, push a PR into develop, and merge only after green CI. Record any Blender GUI verification as manual if unavailable. Do not implement AE revision management in that session.

Later sessions must address AE schema-version gating, optional passes, exact sequence frame coverage and stale labels; revision-safe source updating; expanded QC; the first real release workflow; English/Japanese UX; and a real Blender → After Effects handoff. About metadata still needs a supported write surface. These are known, bounded follow-ups, not an ambiguous repository baseline.
